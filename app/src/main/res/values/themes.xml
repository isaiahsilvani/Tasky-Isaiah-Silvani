<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Tasky" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen theme inheriting from Theme.SplashScreen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#279F70</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/tasky_logo_splash</item>

        <item name="postSplashScreenTheme">@style/Theme.Tasky</item>
    </style>

</resources>
